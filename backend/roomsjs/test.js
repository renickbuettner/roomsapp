// Test TRoom and TView together
/*
App.loadScreen("screen.login");

App.cache._routes.rooms = new App.TRoute("rooms", function () {
    App.loadView(
        new App.TView("RoomContainer", App.getTemplate("template.roomContainer"), {
            "\\$rooms": (new App.TRoom("Raum 905", "Haus 9", {}).getView().html())+(new App.TRoom("Raum 927", "Haus 9", {}).getView().html())
        })
    );
});
App.cache._routes.users = new App.TRoute("users", function () {
    App.loadView(new App.TView("Users", App.getTemplate("template.usermg"), {
        "\\$users": (new App.TView("User", "template.user", {

        }))
    }));
});
*/



// Indev
//App.loadScreen("screen.dashboard");
//App.loadScreen("screen.login");